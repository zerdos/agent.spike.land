# Server Configuration
PORT=3001
FRONTEND_URL=http://localhost:5173

# Note: This backend uses the locally authenticated Claude Code CLI
# No API key needed - ensure 'claude' command is available in PATH
